node_modules/
dist/
# Secrets / local env (never commit real values; see *.env.example)
.env
.env.local
.env.*.local
packages/server/.env
packages/server/.env.test
packages/mobile/.env
*.log
logs/
# DB dumps — contain real data, NEVER commit
backups/
*.sql.gz
.DS_Store
coverage/

# Prisma
packages/server/prisma/*.db

# Expo
packages/mobile/.expo/
packages/mobile/android/
packages/mobile/ios/
packages/mobile/web-build/

# IDE
.vscode/
.idea/

# Claude
.claude/

# 업로드 이미지(로컬 개발용 저장소 — 프로덕션은 도커 볼륨)
packages/server/uploads/

# Terraform (infra/aws) — 상태·변수는 로컬 전용(비밀 포함)
infra/aws/.terraform/
infra/aws/*.tfstate*
infra/aws/terraform.tfvars
infra/aws/.terraform.lock.hcl
